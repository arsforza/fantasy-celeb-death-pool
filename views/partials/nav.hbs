<nav id="navbar" class="navbar navbar-light bg-light position-fixed px-2">
  <div>
  {{#if userInSession}}
    <div class="nav-item">
      <a class="nav-link btn btn-link" href="/dashboard">Home</a>
    </div>
  {{else}}
    <div class="nav-item">
      <a class="nav-link btn btn-link" href="/">Home</a>
    </div>
  {{/if}}
  </div>
  <div class="d-flex flex-row">
    {{#if userInSession}}
      {{#if isAdmin }}
        <div class="nav-item">
          <a class="nav-link btn btn-link" href="/simulate-death">
            Simulate death
          </a>
        </div>
      {{/if}}
      <div class="nav-item">
        <a class="nav-link btn btn-link" href="/user-profile">
          Your bet
        </a>
      </div>
      <div class="nav-item">
        <form action="/logout" method="POST">
          <button class="nav-link btn btn-danger" >Logout</button>
        </form>    
      </div>
    {{else}}
      <div class="nav-item">
        <a class="nav-link btn btn-link" href="/signup">Signup</a>
      </div>
      <div class="nav-item">
        <a class="nav-link btn btn-link" href="/login">Login</a>
      </div>
    {{/if}}
  </div>
</nav>