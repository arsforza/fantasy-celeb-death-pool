<nav>
  {{#if userInSession}}
    <a href="/user-profile">
      {{userInSession.username}}
      {{#if isAdmin }}
        <a href="/simulate-death">Simulate death</a>
      {{/if}}
    </a>
    <form action="/logout" method="POST">
      <button>Logout</button>
    </form>
  {{else}}
    <a href="/signup">Signup</a>
    <a href="/login">Login</a>
  {{/if}}
</nav>